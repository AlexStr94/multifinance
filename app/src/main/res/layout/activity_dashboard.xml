<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    tools:context=".ui.DashboardActivity">

    <!-- Основной контент -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="72dp">

        <LinearLayout
            android:id="@+id/dashboard_container"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- Верхняя панель -->
            <include layout="@layout/view_top_bar" />

            <!-- Приветствие -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Добро пожаловать!"
                android:textColor="@color/primaryText"
                android:textSize="22sp"
                android:textStyle="bold"
                android:layout_marginTop="16dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Ваши финансовые показатели"
                android:textColor="@color/secondaryText"
                android:layout_marginTop="4dp" />

            <!-- Карточка баланса -->
            <LinearLayout
                android:id="@+id/balance_card"
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:orientation="vertical"
                android:padding="16dp"
                android:layout_marginTop="16dp"
                android:background="@drawable/card_surface_rounded">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Общий баланс"
                    android:textColor="@color/secondaryText" />

                <TextView
                    android:id="@+id/tv_balance"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="— ₽"
                    android:textSize="28sp"
                    android:textStyle="bold"
                    android:layout_marginTop="8dp"
                    android:textColor="@color/primaryText" />
            </LinearLayout>

            <!-- Карточки аналитики и продуктов -->
            <include
                layout="@layout/view_analytics_card"
                android:id="@+id/analytics_card" />

            <include
                layout="@layout/view_new_product_card"
                android:id="@+id/new_product_card" />

            <!-- Сообщение о пустых банках -->
            <TextView
                android:id="@+id/tv_no_banks"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="У вас сейчас не добавлено ни одного банка."
                android:textColor="@color/secondaryText"
                android:textSize="16sp"
                android:gravity="center"
                android:layout_marginTop="32dp"
                android:visibility="gone" />

            <!-- Кнопка Добавить банк -->
            <Button
                android:id="@+id/btn_add_bank"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Добавить банк"
                android:layout_marginTop="32dp"
                android:visibility="gone"
                android:backgroundTint="@color/primary"
                android:textColor="@android:color/white"
                android:textStyle="bold"
                android:padding="12dp"
                android:layout_gravity="center_horizontal" />
        </LinearLayout>
    </ScrollView>

    <!-- 🔄 Прогресс-бар поверх контента -->
    <ProgressBar
        android:id="@+id/progress_dashboard"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:visibility="gone"
        android:indeterminate="true" />

    <!-- Нижняя навигация -->
    <include layout="@layout/view_bottom_nav" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
